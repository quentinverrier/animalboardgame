import { Routes } from '@angular/router';
import { SetupHomeComponent } from '../components';

export const setupRoutes: Routes = [
  {
    path: 'setup',
    title: 'Setup',
    component: SetupHomeComponent
  },
];
