export type PlayerServer = {

    alive: boolean;
    boardCards: boolean[];
    canKill: boolean;
    canPlay: boolean;
    handCards: boolean[];
    id: number;
    inactiveCards: boolean[];
    mushrooms: number;
    name: string;

}